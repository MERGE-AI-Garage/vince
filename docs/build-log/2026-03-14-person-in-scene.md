# Person-in-Scene: Headshots & Video with a Real Person

How to place a specific person's face and likeness into AI-generated images and videos using Vince.

---

## The Problem This Solves

Standard image/video generation tools create fictional people. When you upload a headshot and ask Vince to "put me in this scene," the default generation tools (`generate_image`, `generate_creative_package`, `generate_video`) cannot reproduce a specific real person's face — they generate a plausible stand-in.

`generate_headshot_scene` exists specifically to solve this using Gemini's image editing model with face-preservation constraints.

---

## Tool: `generate_headshot_scene`

**Model:** `gemini-3.1-flash-image-preview` with `responseModalities: ['IMAGE']` only

**What it does:** Takes a reference photo and a scene description, returns a new image with the same face placed into the described scene. Background, clothing context, environment, and lighting change; facial features, skin tone, eye color, and hair style are preserved.

> **Why IMAGE-only?** Passing inline image data with TEXT+IMAGE interleaved mode causes Gemini to drop image generation entirely (~2s response, 0 images). IMAGE-only mode is the correct path for image editing/transformation.

### Parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `photo_url` | Yes | URL of the reference photo (Supabase storage or public URL) |
| `scene_description` | Yes | Vivid description of the target environment, setting, lighting, mood |
| `aspect_ratio` | No | Default `4:5`. Supports standard ratios. |

### Example prompt to Vince

> "Put me in a modern conference room in business casual attire, soft window lighting, professional headshot style"

> "Place me at a rooftop event in NYC at golden hour, wearing a blazer, candid style"

### Auto-injection

If you have uploaded a reference photo in the conversation (the chat shows `[Reference images uploaded by user: ...]`), Vince will automatically use that URL as `photo_url` — you don't need to paste the URL manually.

---

## Creative Package With a Real Person: Two-Step Flow

When the user uploads a headshot and asks for a creative package (LinkedIn post, social content, etc.), Vince runs a two-step chain to deliver both the person-in-scene photo AND the branded copy:

```
generate_headshot_scene(photo_url, scene_description, aspect_ratio)
  → returns output_url (the person placed in the scene)

generate_creative_package(brief, deliverables, pre_generated_image_url=<output_url>)
  → generates only written copy (gemini-2.0-flash, TEXT modality)
  → attaches output_url as the deliverable image — no duplicate generation
```

The `pre_generated_image_url` parameter tells `generate-creative-package` to skip Gemini image generation and use the supplied URL as the visual. The copy is generated by `gemini-2.0-flash` in TEXT-only mode — faster and cheaper than the image-preview model.

Vince is instructed to perform this chain automatically when a headshot is in context and a creative package is requested.

---

## Video With a Real Person: Character Reference Collections (Preferred)

Veo 3.1 supports up to 3 reference images passed as `referenceType: "asset"` in the `referenceImages` API field — the same mechanism Google Flow uses for "ingredients." Passing multiple angles of the same person improves face consistency over a single image.

**The preferred approach:** create a character reference collection in Brand Settings with 2-3 headshots (different angles/lighting), then ask Vince to use it:

> "Make a video of me at Google Next — use my Kurt Miller reference collection"

Vince will call `generate_video` with `reference_collection="kurt miller"`, resolve all images in the collection from the database, and pass them to Veo as `reference_images[]`. The quality model is required and selected automatically.

The prompt should describe the person as "the person from the reference images" to connect the reference subjects to the scene.

### Fallback: Two-Step Flow

If no character collection exists and only a single headshot is available, Vince falls back to a two-step chain:

```
generate_headshot_scene(photo_url, scene_description)
  → [returns output_url: a generated still with the face placed in the scene]

generate_video(generation_type="image_to_video", input_image_url=<output_url>)
  → [animates the still — face is preserved because it was in the starting frame]
```

This is more reliable for face preservation when only one image is available, since the face is embedded in the starting frame rather than provided as a reference.

> "Make a video of me walking into that conference room — use my headshot"

---

## Known Limitations & Behavior

| Issue | Status |
|-------|--------|
| Face preservation is best-effort — Gemini safety filters may block images with prominent faces | Active; see Supabase logs for `finishReason`/`blockReason` |
| Gemini may return `SAFETY` finish reason with no image even for benign professional portraits | Mitigated by clear professional prompts; avoid ambiguous language |
| Headshot filename matters — uploading a file named `klm.jpg` may cause Vince to interpret "KLM" as an airline brand name | Rename headshot files to `headshot.jpg`, `portrait.jpg`, or your name |
| `generate_creative_package` cannot place a specific person's face | Handled via two-step pattern: generate_headshot_scene → generate_creative_package(pre_generated_image_url) |

---

## Prompting Tips

**For best face preservation:**
- Keep the scene description professional and clear
- Mention lighting style (soft, natural, studio, golden hour)
- Mention clothing context (business casual, blazer, casual) — this guides the model without instructing it to change the face
- Avoid language that could be interpreted as altering facial features ("make me look younger", "change my expression")

**For video:**
- Prefer a character reference collection (2-3 angles) over a single headshot — more images = better consistency
- Reference the person in the prompt as "the person from the reference images"
- If using the two-step fallback, the `generate_headshot_scene` scene description should match the environment you want in the video
- Use 8-second duration for video; the first ~2 seconds are often the clearest frame

---

## Debugging

If `generate_headshot_scene` fails, Vince will say something like "I ran into an issue generating that headshot scene." Check Supabase edge function logs (`brand-prompt-agent`) for a line matching:

```
[generateHeadshotScene] No image returned. finishReason: ... | blockReason: ...
```

Common values:
- `finishReason: SAFETY` — Gemini safety filter triggered; try rephrasing the scene description
- `blockReason: SAFETY` — prompt itself was blocked before generation; simplify the scene description
- HTTP 400/500 — API key, model name, or request format issue

---

## Implementation Notes

- Function: `brand-prompt-agent/index.ts` → `generateHeadshotScene()`
- Photo download: Uses Supabase service-role storage client for `creative-studio` bucket URLs; bare `fetch()` fallback for external URLs
- Base64 conversion: Chunked 8192-byte loop (avoids O(n²) string concatenation on large images)
- Output storage path: `creative-studio/headshot-scenes/{user_id}/{filename}.{ext}`
- Gemini endpoint: `gemini-3.1-flash-image-preview:generateContent`
- Response modality: `IMAGE` only — TEXT+IMAGE with inline image input causes Gemini to return text-only (~2s, 0 images); IMAGE-only is required for image editing/transformation
- Prompt pattern: "Transform this person's photo: [scene description]. CRITICAL REQUIREMENTS: Keep the same face..." — face preservation prompt
- `pre_generated_image_url` in `generate-creative-package`: when set, generates copy only (gemini-2.0-flash TEXT mode) and attaches the supplied URL as the deliverable image
- History tracking: inserts to `creative_studio_generations` (type: `headshot_scene`) and `media` table on success
- DB constraint: `headshot_scene` added to `creative_studio_generations_generation_type_check` via migration `add_headshot_scene_generation_type`
